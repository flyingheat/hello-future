集合：
    -集合在java中时容器的一种，用以储存数据，每一层集合底层都封装了对应的数据结构

   -数据结构：数组，链表，队列，枚举，哈希表，二叉数

    -学习java集合不要求你严格掌握数据结构

    list.get()方法是list接口独有的方法，可以按照下表索引来获取指定元素

    -collection接口是集合的一个父接口，里面提供了集合的一些基本操作方法，比如添加元素，
    删除元素，获取集合元素，清空集合等

    -list接口  （有序可以重复）
         -可以根据下表索引来获取指定位置
          -list集合可以存放重复的数据
          -实现类 ArrayList，LinkList
              -ArrayList底层封装的是数组，数组查询效率高，所及增删效率低
                 -是线程不安全的的


              -LinkList底层封装的是链表曼联表查询效率低，随机增删效率高
     -set接口（无序不可重复）

     -集合中的泛型机制
           java5之后的泛型机制，指定泛型的目的是为了强制集合只能存放某一种数据类型的实例
           -<数据类型> 钻石表达式
               - ArrayList<Stuent> arrayStu = new ArratList<>();
            -我们可以自定义泛型


